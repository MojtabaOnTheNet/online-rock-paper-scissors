services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    depends_on:
      - redis
    restart: unless-stopped

  redis:
    image: redis:8
    restart: unless-stopped
    volumes:
      - redis-data:/data

volumes:
  redis-data:
